- @page_header = "Add a new audio book"
.row
  .col-md-4.col-md-offset-4
    = form_tag do
      .form-group
        = label_tag "Title"
        = text_field_tag "Title"
      = submit_tag "Submit", class: "btn btn-primary"
